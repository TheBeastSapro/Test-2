# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
screenshots/

# The review clips are source assets, not build artifacts — the repo root
# ignores *.mp4, so re-include them explicitly or the H.264 encode ships
# missing and Safari/Chrome get a 404.
!public/clips/*.mp4

# Local database and uploads — never commit these
data/
