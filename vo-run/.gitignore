# Generated audio and per-run work dir — rebuildable, and large.
out/
unlevelled.wav
excerpts/

# Stage logs are appended to live by a running job, so tracking them means
# every turn sees a dirty tree mid-write. A stage's log is copied to
# <stage>.final.log once it finishes, and that copy is what gets committed.
*.log
!*.final.log
